const Spinner = () => {
  return (
    <div
      role="status"
      className="inline-block h-8 w-8 animate-spin rounded-full border-4 border-solid border-r-red"
    />
  );
};

export default Spinner;
